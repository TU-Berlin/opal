IMPLEMENTATION TestOpalCompiler
IMPORT BasicIO     ONLY writeLine:nat->com[void]
       Com         ONLY com
       ComCompose  ONLY &
       Nat         ONLY nat
       ProcessArgs ONLY argCount
DEF main == argCount & writeLine
