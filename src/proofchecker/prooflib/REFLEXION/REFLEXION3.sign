/* %Reflexions on properties. */

SIGNATURE REFLEXION3[law:LAW]

LAW law == "dummy"

-- %$For denoting Laws in Proofs$

-- %The restricted law @code{Restr[l]}.
LAW Restr == "dummy"

-- %The copied law @code{Copy[l]}
LAW Copy == "dummy"

-- %$Abstract Syntax$

-- %@i{need these imports, so the evaluator knows how to print annotations}
IMPORT RepoAbsy COMPLETELY
       RepoSource ONLY fmtLocator locator:SORT

FUN PROPERTY: property

