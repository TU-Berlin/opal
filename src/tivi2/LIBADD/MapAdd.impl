IMPLEMENTATION MapAdd

IMPORT MapNotForUserPurpose COMPLETELY
       Seq ONLY seq
       SeqMap ONLY *
       Pair COMPLETELY     
       SetByBSTConv ONLY set asSeq

DEF codom(M) == 2nd * asSeq(rep(M))
