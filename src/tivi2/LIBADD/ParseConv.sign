/* %Convert a preparse function to a parse function. */
SIGNATURE ParseConv[data]

SORT data

IMPORT Option[data] ONLY option

FUN asParse: (denotation -> option[data] ** denotation) ->
             denotation -> option[data]
