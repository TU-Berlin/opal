IMPLEMENTATION NatMapMap

IMPORT NatMap		COMPLETELY
IMPORT SeqMap		COMPLETELY
IMPORT Seq		COMPLETELY
IMPORT Pair		COMPLETELY

FUN * : (from -> to) ** natMap[from] -> natMap[to]
DEF F * Map == implode((\\P. &(1st(P), F(2nd(P)))) * explode(Map))
