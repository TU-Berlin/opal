/* Formatting Basic Types - currently only @code{nat} and @code{string}. */

SIGNATURE FormatBasicTypes

IMPORT Format ONLY fmt
       Nat ONLY nat
       String ONLY string

FUN fmtNat: nat -> fmt
FUN fmtString: string -> fmt
