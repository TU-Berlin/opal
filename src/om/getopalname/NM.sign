/* %This strutcure provides a command which reads a single 
    argument from the command line and outputs the 
    internally used C identifier. This is necessary for the
    @code{ocs -top} command, so that the linker knows
    the C identifier of the top Opal command.
*/

SIGNATURE NM

IMPORT Com[void] ONLY com
       Void ONLY void

FUN getopalname: com[void]
