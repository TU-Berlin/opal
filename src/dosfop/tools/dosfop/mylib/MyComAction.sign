SIGNATURE MyComAction

IMPORT Com[void] ONLY com
       Void ONLY void

/* %"Cut": Break with message, if boolean yields true. */
FUN -!-> : bool ** (() -> denotation) -> com[void]

/* %Perform command, if boolean yields true. */
FUN -?-> : bool ** (() -> com[void]) -> com[void]
