/* % @b{Dynamic Sort Information}
     This structure will become @code{THEORY Dynamic} in @sc{Opal-2}.
     It makes runtime type information for sorts available to the
     user. */

SIGNATURE Dynamic[alpha]

/$ DYNAMIC [alpha] $/

SORT alpha


IMPORT Reflection ONLY sort : SORT


FUN reflectSort : sort
