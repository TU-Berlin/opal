SIGNATURE ReflectionRepository

IMPORT Cache[denotation, <, option[interOpal]] ONLY cache : SORT
       Denotation ONLY <
       InterOpal ONLY interOpal : SORT
       Option[interOpal] ONLY option


/* % This cache provides fast access to interOpal files. Each
     structure will only be loaded once per program runtime.
     A value of @code{nil} is returned if the interOpal file could not 
     be loaded. */

FUN interOpalRepository : cache



