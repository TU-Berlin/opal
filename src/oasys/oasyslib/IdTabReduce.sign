/* % This structure provides reductions on @code{idtab'IdTab}. */

SIGNATURE IdTabReduce[codom, to]

SORT codom to

IMPORT  Id              ONLY id:SORT
        IdTab[codom]    ONLY idtab:SORT

FUN /   : (codom ** to -> to) ** to ** idtab[codom] -> to
FUN /   : (id ** codom ** to -> to) ** to ** idtab[codom] -> to
