SIGNATURE ArgspecIntern

-- %- ======================================
-- % not for user purpose 

IMPORT	Com[set[denotation,<]]	ONLY	com
	Set[denotation,<]	ONLY	set
	Denotation		ONLY	<
	Seq[denotation]		ONLY	seq

TYPE cxt == 
	fail
	done
	stop(completer: denotation	-> com[set[denotation,<]])

