/* % This structure provides mappings on @code{idmap'IdMap}. */

SIGNATURE IdMapMap[codom1, codom2]

SORT codom1 codom2

IMPORT  Id              ONLY id:SORT
        IdMap[codom1]   ONLY idmap:SORT
        IdMap[codom2]   ONLY idmap:SORT

FUN *   : (codom1 -> codom2) ** idmap[codom1] -> idmap[codom2]
