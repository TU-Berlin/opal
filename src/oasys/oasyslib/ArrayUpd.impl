IMPLEMENTATION ArrayUpd[data:SORT]

IMPORT 	Array		COMPLETELY
	Nat		COMPLETELY
	Seq[data]	COMPLETELY

FUN := : array[data] ** nat ** seq[data] -> array[data]
DEF (A,_) := <> == A
DEF (A,n) := (Ft::Rt) == ((A,n) := Ft,succ(n)) := Rt
