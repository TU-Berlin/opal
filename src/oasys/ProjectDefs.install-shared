## ProjectDefs for bootstrapping the Oasys shared library
## This file just ensures that full optimization is used

# Default variant selection 

ocs		:= stdocs	# stdocs expocs
opt		:= full		# no modest medium full
debug		:= no		# no opal c
profile		:= no		# no gprof

REL_GENOPTIONS += -keep