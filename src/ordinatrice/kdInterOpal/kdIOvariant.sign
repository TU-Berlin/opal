SIGNATURE kdIOvariant

  IMPORT InterOpal ONLY interOpal:SORT name:SORT variant:SORT
			< : name ** name -> bool
	 Set[name,<] ONLY set

/* % return set of the used sorts in the constructors */
  FUN sortsOf: interOpal -> variant -> set[name,<]
